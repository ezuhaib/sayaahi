<div id="entryform">
  <%= render "form" %>
</div>

<div id="entries">
  <table class="table table-striped">
    <tbody>
      <% @entries.each do |entry| %>
        <tr>
          <td><%= entry.body %></td>
          <td><%= link_to entry.user.username , entry.user %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
